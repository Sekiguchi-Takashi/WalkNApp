# WalkNApp オントロジー設計書 v2.0

装備型ウォーキングアプリ（STEPN型速度ゲート＋時間報酬）
更新: 2026-08-22 / 対象: Phase 1（NFT連携は Phase 2）

---

## 1. 設計の中心

v1.x は「場所」が中心（地図上のスポーンを拾う）だったが、v2.0 では
**装備 × 有効時間** が中心になる。地図はルート記録の表示装置に役割を変える。

報酬が発生する条件は一つだけ:
**装備した靴の速度帯に収まっている時間（＝有効時間）が、装備したウェアのしきい値に達すること。**

---

## 2. オントロジー全体図

```
WalkNApp
├─ Walker                 歩く人（端末内単一）
│   ├─ Loadout            装備セット（靴1・ウェア1）
│   ├─ DailyQuota         その日の獲得上限と消化状況
│   └─ Streak             連続達成日数
│
├─ Equipment              装備
│   ├─ Shoe               靴（速度帯・耐久）
│   └─ Wear               トレーニングウェア（しきい値・付与ランク）
│
├─ WalkSession            スタート〜ストップの1回
│   ├─ RouteTrack         軌跡（座標列）
│   ├─ SpeedSample        速度サンプル
│   ├─ ValidWalkTime      有効時間（速度帯内の累積）
│   └─ SessionProgress    連続有効時間と次回付与までの残り
│
├─ Asset                  獲得アイテム（Phase 2 で NFT 化）
│   ├─ ItemRank           LOW / MID / HIGH
│   └─ RepairPoint        内包する修理ポイント
│
├─ PointLedger            ポイントの収支記録
│   ├─ Earn               アイテム獲得による加算
│   ├─ Consume            靴の消耗による減算
│   └─ Overflow           上限超過による消滅
│
└─ Event                  イベント履歴（Phase 2 の AI Engine 入力）
```

---

## 3. エンティティ定義

### 3.1 Shoe（靴）

| 属性 | 型 | 説明 |
|---|---|---|
| id | String | 個体ID（UUID） |
| type | ShoeType | STROLLER / WALKER / SPEEDWALKER |
| durability | Int | 0〜100 |
| equipped | Boolean | 装備中フラグ |
| totalValidSec | Long | この靴での累計有効時間 |

**ShoeType**

| type | 名称 | 有効速度 | 想定 |
|---|---|---|---|
| STROLLER | ストローラー | 2.0〜4.5 km/h | 散歩・買い物 |
| WALKER | ウォーカー | 4.0〜7.0 km/h | 通常の歩行 |
| SPEEDWALKER | スピードウォーカー | 6.0〜9.5 km/h | 早歩き |

- 速度帯は隣と重なる（4.0〜4.5、6.0〜7.0）。境目でどちらでも成立する余地を残す
- 耐久0で装備不可（他の靴に履き替えるか修理が必要）

### 3.2 Wear（トレーニングウェア）

| 属性 | 型 | 説明 |
|---|---|---|
| type | WearType | LIGHT / STANDARD / SERIOUS |
| thresholdMin | Int | 付与に必要な連続有効時間（分） |
| grantRank | ItemRank | 付与されるランク |
| equipped | Boolean | 装備中フラグ |

| type | 名称 | しきい値 | 付与ランク | 1回の獲得 |
|---|---|---|---|---|
| LIGHT | 軽装 | 5分ごと | LOW | 1pt |
| STANDARD | 標準 | 15分ごと | MID | 2〜5pt |
| SERIOUS | 本格 | 30分ごと | HIGH | 5〜10pt |

- ウェアは消耗しない（靴だけが消耗する）
- セッション中の変更は不可（次のセッションから反映）

### 3.3 WalkSession

| 属性 | 型 | 説明 |
|---|---|---|
| id | Long | セッションID |
| startAt / endAt | Long | 開始・終了時刻 |
| shoeId / wearType | - | 開始時点の装備を固定記録 |
| totalSec | Long | 経過時間 |
| validSec | Long | 有効時間（速度帯内の累積） |
| continuousValidSec | Long | 連続有効時間（付与判定に使用） |
| distanceM | Double | 距離 |
| steps | Int | 歩数 |
| routeJson | String | 軌跡 |
| grants | Int | このセッションでの付与回数 |
| durabilityUsed | Int | 消費した耐久 |

### 3.4 ValidWalkTime（有効時間）

判定ロジック:
```
移動平均速度（30秒窓）を算出
  ↓
装備中の靴の速度帯に収まっているか
  ├─ Yes → validSec += 経過秒 / continuousValidSec += 経過秒
  └─ No  → validSec 停止 / continuousValidSec = 0（リセット）
```

- 速度帯を外れた瞬間に**連続有効時間はリセット**される
- これが「細切れに歩くと損」の中核。15分×4回では SERIOUS の30分に届かない
- 猶予: 信号待ち等を考慮し、速度帯を外れて **60秒以内に復帰すれば連続を維持**する（それを超えるとリセット）

### 3.5 SpeedState

| 状態 | 条件 | UI |
|---|---|---|
| TOO_SLOW | 速度帯下限未満 | 「もう少し速く」 |
| OPTIMAL | 速度帯内 | 有効時間が進行 |
| TOO_FAST | 速度帯上限超過 | 「速すぎます / 靴を替えますか」 |
| GRACE | 帯外だが60秒以内 | 「あと○秒で連続が途切れます」 |

### 3.6 Asset（獲得アイテム）

v1.x の AssetEntity を踏襲。以下を追加:

| 追加属性 | 説明 |
|---|---|
| rank | LOW / MID / HIGH |
| repairPoint | 内包する修理ポイント |
| earnedAtValidSec | 獲得時点の連続有効時間 |
| shoeType / wearType | 獲得時の装備 |
| avgSpeedKmh | 獲得直前30秒の平均速度 |

- **座標は保持するが、獲得条件ではない**（記録・Phase 2 メタデータ用）
- 地図上にアイテムは表示しない。獲得は自動

### 3.7 DailyQuota

| 属性 | 説明 |
|---|---|
| date | 日付（端末ローカル） |
| earnedPoints | その日の獲得ポイント |
| capBase | 基本上限 18 |
| streakBonus | 連続達成ボーナス 0〜10 |
| cap | capBase + streakBonus |

- 上限に達すると以降の付与は発生しない（Overflow として記録のみ）
- 日付が変わるとリセット

### 3.8 Streak

| 属性 | 説明 |
|---|---|
| streakDays | 連続達成日数 |
| lastAchievedDate | 最後に60分達成した日 |

- **達成条件: 1日の有効時間が合計60分以上**
- 達成した翌日から上限 +2、最大 +10（5日で cap 28）
- 1日でも未達なら streakDays = 0、cap は 18 に戻る

### 3.9 PointLedger

| kind | 発生源 | 符号 |
|---|---|---|
| EARN | アイテム獲得 | + |
| CONSUME | 靴の消耗（有効時間5分ごとに1） | − |
| REPAIR | 修理適用 | 靴の durability に + |
| OVERFLOW | 上限超過・耐久100超過 | 消滅（記録のみ） |

---

## 4. 経済バランス

### 消耗
**有効時間5分ごとに靴の耐久 −1**

| 有効時間 | 消耗 |
|---|---|
| 30分 | 6 |
| 60分 | 12 |
| 90分 | 18 |

### 収支（60分・上限18）

| ウェア | 付与回数 | 獲得 | 消耗 | 純増 |
|---|---|---|---|---|
| LIGHT | 12回 | 12 | 12 | ±0 |
| STANDARD | 4回 | 約14 | 12 | +2 |
| SERIOUS | 2回 | 約15 | 12 | +3 |

### 時間別（SERIOUS）

| 有効時間 | 獲得 | 消耗 | 純増 |
|---|---|---|---|
| 30分 | 7.5 | 6 | +1.5 |
| 60分 | 15 | 12 | +3 |
| 90分 | 18（上限） | 18 | ±0 |

**60分が最適点**。それ未満は取りこぼし、それ以上は上限に当たって赤字化する。

### 靴の寿命
100ポイント ÷ 1pt/5分 = **有効時間 500分（約8.3時間）**
60分/日なら 8日強でゼロ。修理し続ける前提の設計。

---

## 5. 60分歩く動機の3層

| 層 | 仕組み | 効果 |
|---|---|---|
| 短期 | 連続有効時間でしきい値判定、中断でリセット | まとめて歩く |
| 中期 | 60分達成で翌日から上限+2、最大+10 | 毎日続ける |
| 長期 | 耐久0で靴が使用不可、超過分は消滅 | 資産維持のため歩き続ける |

---

## 6. 状態遷移

```
[IDLE]
  │ スタート
  ▼
[ACTIVE] ──速度帯内──> validSec/continuousValidSec 加算
  │  │
  │  └──帯外──> [GRACE]（60秒）──復帰──> [ACTIVE]
  │                  │
  │                  └──60秒超過──> continuousValidSec = 0
  │
  │ しきい値到達 かつ 日次上限未達
  ▼
[GRANT] アイテム付与 → 修理ポイント加算 → continuousValidSec = 0（次の周期へ）
  │
  ▼
[ACTIVE]
  │ ストップ
  ▼
[ENDED] セッション保存 / 60分達成なら Streak 更新
```

---

## 7. 画面構成（v2.0）

| 画面 | 内容 |
|---|---|
| ウォーク（メイン） | 地図＋軌跡。速度・SpeedState・有効時間・次の付与までの残り時間・本日ポイント/上限。スタート/ストップ |
| 装備 | 靴3種の耐久と速度帯、ウェア3種。履き替え。修理 |
| 持ち物 | 獲得アイテム一覧（ランク・修理ポイント・獲得時装備・速度） |
| 履歴 | セッション一覧（有効時間・距離・付与数・消耗） |
| 図鑑/統計 | 累計、連続日数、上限の推移 |

---

## 8. v1.x からの廃止・変更

| 対象 | 扱い |
|---|---|
| SpawnEngine / SpawnPoint | **廃止**（座標スポーンを行わない） |
| 地図マーカー・タップ取得 | **廃止**（自動付与に一本化） |
| 決定論スポーン（セル＋日付シード） | **廃止** |
| 歩数ボーナス（3000/6000/10000歩） | **廃止**（有効時間に統合） |
| ItemCatalog | ランク別に再編（LOW/MID/HIGH に整理） |
| SessionTracker / SpeedFormat | **継続**（速度判定の中核に昇格） |
| RouteTrack / ポリライン表示 | **継続** |
| 15km/h 除外判定 | SpeedState に統合 |
| Asset / Metaplex エクスポート | **継続**（属性を追加） |

---

## 9. Phase 2 への接続

Phase 2 の Universal Asset Platform 設計に対し、v2.0 は以下を提供する:

- Asset に「どんな装備で、どれだけ連続して歩いて得たか」が記録される
  → cNFT メタデータの attributes として来歴価値になる
- Shoe 自体が将来 NFT 化の候補（STEPN 型の資産）。ShoeType と durability は
  そのままメタデータに載る構造にしておく
- PointLedger は Marketplace / Exchange の価値算定の基礎データになる
- DailyQuota / Streak は AI Engine の提案材料（「あと○分で上限」等）

---

## 10. 確定した仕様

### 10.1 靴の入手（初期配布）

- 初回起動時に3種の靴を**各1足ずつ配布**（耐久100）
- Phase 1 では追加入手の手段を設けない
- 途中入手（ドロップ・クラフト・購入）は将来対応。Shoe は個体IDを持つので
  複数所持できる構造にしておく

### 10.2 修理（手動）

- 獲得した修理ポイントは **RepairWallet** に貯まる（靴に自動適用しない）
- 装備画面で靴を選び、投入量を指定して修理する
- 耐久の上限は100。超過投入は不可（入力時にクランプ）
- RepairWallet 自体に上限はないが、日次獲得上限（cap）が入口を絞る

| 追加エンティティ | 属性 |
|---|---|
| RepairWallet | balance: Int |
| RepairEvent | shoeId, amount, at, beforeDurability, afterDurability |

### 10.3 GPS精度が悪い場合の救済

**2段構えの速度推定**

| 優先度 | ソース | 使用条件 |
|---|---|---|
| 1 | GPS（移動平均30秒） | 位置精度が良好（accuracy ≦ 30m） |
| 2 | 歩数センサー × 推定歩幅 | GPS が不良または未取得 |

推定式:
```
推定速度(m/s) = ケイデンス(歩/秒) × 推定歩幅(m)
```

**推定歩幅の学習（履歴からの推測）**

- GPS が良好だったセッション区間から `距離 ÷ 歩数` を収集
- ケイデンス帯（低/中/高）ごとに歩幅の中央値を保持
  → 速く歩くほど歩幅が伸びる個人差を反映できる
- サンプルが集まるまでは既定値（身長未入力なら 0.70m）を使用
- 外れ値（0.3m 未満 / 1.2m 超）は学習から除外

| 追加エンティティ | 属性 |
|---|---|
| StrideProfile | cadenceBand, strideM, sampleCount, updatedAt |
| SpeedSource | GPS / STEP_ESTIMATE |

- 獲得した Asset には speedSource を記録し、GPS由来か推定由来かを区別する
  （Phase 2 のメタデータ信頼度に使う）

### 10.4 屋内・トレッドミル

**有効時間は進まない。** これは仕様であり不具合ではない。

判定:
- 歩数は増えているが、**GPSの実移動距離が直近5分で50m未満**の場合は
  トレッドミル/足踏みとみなし、STEP_ESTIMATE による救済を適用しない
- SpeedState = INDOOR とし、UIに「屋外のウォーキングが対象です」と表示

---

## 11. 10.3 と 10.4 の関係（重要）

歩数センサー救済（10.3）とトレッドミル除外（10.4）は方向が逆で、
両立させるには「GPSが不良」と「GPSは正常だが移動していない」を区別する必要がある。

| GPS状態 | 実移動 | 歩数 | 判定 |
|---|---|---|---|
| 良好 | あり | あり | GPS で計測（通常） |
| 良好 | なし | あり | **INDOOR**（トレッドミル・足踏み）→ 有効時間停止 |
| 不良/未取得 | 不明 | あり | STEP_ESTIMATE で救済 |
| 不良/未取得 | 不明 | なし | 停止扱い |

STEP_ESTIMATE による救済は**連続10分を上限**とし、それを超えたら停止する。
GPSが取れないまま延々と歩数だけで加算されると、実質トレッドミルを許容してしまうため。

---

## 12. 残る検討事項

- 初期配布の3足を「同時に持つ」か「1足選ばせる」か（現案は3足配布）
- RepairWallet の残高が余った場合の使い道（将来の靴入手コストに充てる想定）
- StrideProfile の学習に必要な最小サンプル数（暫定: ケイデンス帯ごとに20区間）
